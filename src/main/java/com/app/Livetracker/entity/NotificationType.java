package com.app.Livetracker.entity;

public enum NotificationType {
    ORDER_PLACED,
    RIDER_ASSIGNED,
    RIDER_REQUEST,
    PICKED_UP,
    OUT_FOR_DELIVERY,
    REACHED_DESTINATION,
    DELIVERED,
    RIDER_EXPIRED
}
